# Session: PDPA re-verification and real-data untracking

**Date**: 2026-06-21
**Branch**: Fixers
**Session type**: Independent re-validation + remediation (owner-authorised)

---

## Objective

Owner ran a 7-phase recovery/re-validation prompt: re-verify all alleged security
findings independently, re-run test gates, then plan and (after approval) execute
fixes. The headline outcome: a prior finding was overturned and the real exposure
was identified and removed from tracking.

---

## What was overturned (false positive)

The 2026-06-15 handoff flagged four `@iium.edu.my` emails in
`Archive/Historical_Screenshots/audit_screenshots/*.json` as a HIGH-severity PDPA
breach of real personal data. This was wrong.

Evidence: `lakshmi.krishnan@`, `ahmad.hassan@`, `supervisor.gombak@`,
`fatimah.abdullah@` are synthetic personas defined in
`database/seeders/TestingGuideDataSeeder.php` (lines 93-161) with fabricated names
("Dr. Lakshmi a/p Krishnan") and demo passwords (`Admin@2024!`). The audit JSONs
contain only these seeded accounts, `codex.*@uat.creams.test` generated accounts,
and org public contact addresses. None of the genuinely real records appear there.

Owner confirmed directly: the ONLY real-life data is in the IRL files; everything
else is synthetic or semi-synthetic.

---

## The genuine exposure (found and remediated)

`Archive/Legacy_Exports/IRL_Files/` — 49 real PDK centre artefacts (facility and
attendance photos, asset invoices, forms; ~64 MB) tracked in git **only on Fixers**
(`main` and `dev` never had them). Also `database/real_data_backup.json` — a DB dump
(synthetic per owner, but carries bcrypt hashes) generated by `GombakDataExtractor`.

### Actions taken

| Action | Detail |
|---|---|
| `git rm --cached` IRL_Files (49) + real_data_backup.json | Local copies kept on disk |
| `.gitignore` | Added IRL_Files/, irl_reference_materials_DUPLICATE/, real_data_backup.json |
| `database/seeders/GombakDataExtractor.php` | Env-gated to `APP_ENV=local` (matches IRLSeeder); owner authorised the source edit |
| `.githooks/pre-commit` | Added path-based blocklist (content-pattern loop missed these — no IC/password literals) |
| Docs | Corrected `01_CURRENT_STATUS.md` blocker #2; added "do not repeat" notes |

---

## Test state (VERIFIED this session)

| Suite | Result |
|---|---|
| PHPUnit (before edits) | 377 passed / 0 failed (91s) |
| PHPUnit (after edits) | 377 passed / 0 failed (37.5s) |
| Playwright | NOT re-run this session; last verified 2026-06-14 (215/3 skip/0) |

PHP lint on the edited seeder and `bash -n` on the hook both pass.

---

## Remaining owner-only step

The 64 MB of IRL files remain in Fixers git **history** and on `origin/Fixers`
(public repo). Full purge requires `git filter-repo` + force-push (rewrites history,
breaks existing clones). `main`/`dev` are clean, so merging Fixers to main after this
commit does not carry the files. Optional stronger containment: set the GitHub repo
private before the history rewrite.

---

## Other confirmed findings (from owner's own CRITICAL_FINDINGS_REGISTER, not changed)

- CF-06 IRLSeeder: real data, hard-gated to local — gate verified present.
- CF-07: 72 IC patterns in git history — deferred (BFG/filter-repo, owner-coordinated).
- CF-08: MainController logs `session()->all()` PII at debug — safe only if production
  `LOG_LEVEL=warning`. Verify before any production push.
