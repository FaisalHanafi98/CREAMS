# CREAMS — Critical Findings Register

> **Generated**: 2026-05-31
> **Purpose**: Capture all critical discoveries made during repository exploration that may affect security, PDPA compliance, deployment, operational safety, or future remediation planning.
> **Status**: Document only — no fixes applied.
> **Use**: This register will be referenced during later security and remediation phases.

---

## Register

| # | Finding | Location | Risk Level | Category | Evidence | Recommended Future Audit |
|---|---------|----------|------------|----------|----------|--------------------------|
| CF-01 | **Real production data backup on disk** — 1,801-line JSON containing 1 real centre (Gombak), 14 real users with names/emails, and 57 real assets. Exists outside any `.gitignore` pattern. | `database/real_data_backup.json` | **CRITICAL** | PDPA, Credentials | JSON contains `centre_name: "Gombak"`, user records with real Malaysian names, email addresses, and asset inventory data. File size: ~60KB. Generated by `GombakDataExtractor.php`. | Verify if committed: `git log -- database/real_data_backup.json`. If committed, plan BFG history rewrite. Immediate: add to `.gitignore`. Delete the extractor or env-gate it. |
| CF-02 | **Live UAT audit screenshots expose real production email** — `lakshmi.krishnan@iium.edu.my` visible in `gate-results.json` from `pdk-creams.org` | `docs/audit/screenshots/live-uat-gate-20260516T163536Z/gate-results.json` | **CRITICAL** | PDPA, Security | JSON files contain full redirect chains, `set-cookie` headers, and real user identifiers from production domain. Multiple similar files exist under `docs/audit/screenshots/`. | Full scan of `docs/audit/screenshots/` for PII. Add to `.gitignore` OR sanitize all JSON files. Audit all `live_*_uat_*` reports for embedded PII. |
| CF-03 | **Hardcoded database passwords in deploy script** — `ProdPassword123!`, `StagingPassword123!`, `DevPassword123!` in plaintext | `scripts/server-init.sh` (MySQL user creation lines) | **CRITICAL** | Credentials, Deployment | Script creates 3 MySQL users with hardcoded passwords, grants full privileges, and writes `.env` files containing these credentials to disk. | Audit whether script was ever run on a live server. If yes, rotate all 3 MySQL passwords immediately. Replace with `$PROD_DB_PASSWORD` / `$STAGING_DB_PASSWORD` / `$DEV_DB_PASSWORD` environment variable references. |
| CF-04 | **Two full repository copies in `.claude/worktrees/`** — each duplicates all files including `real_data_backup.json`, `.env` variants, and full git history | `.claude/worktrees/competent-jepsen-88ca88/` and `.claude/worktrees/nifty-tereshkova-2974e6/` | **CRITICAL** | PDPA, Security, Infrastructure | Each worktree is a full `git worktree` copy of the repo at some prior state. Duplicates all sensitive data. Not listed in `.gitignore`. Both worktrees contain copies of `real_data_backup.json`. | Are worktrees still needed? If not, `git worktree prune`. If needed, verify no real data remains. Add `.claude/worktrees/` to `.gitignore` if not intended to be committed. |
| CF-05 | **4 old `.env` files with exposed `APP_KEY` values** — multiple archive copies contain real application keys | `archive/cream/.env`, `archive/Code VSC/CREAM/.env`, `archive/Code VSC/creams/.env`, `archive/Code VSC/creamtest1/.env` | **HIGH** | Credentials, Security | Each `.env` contains `APP_KEY=base64:...` (real keys), `DB_HOST=127.0.0.1`, `DB_USERNAME=root`, `DB_PASSWORD=` (empty). Passwords are gone but keys remain in plaintext. | Verify if any key matches a key still in use. If not, archive keys are low-risk (old local dev). If any matches live key, rotate immediately. Add all archive `.env` files to `.gitignore`. |
| CF-06 | **IRLSeeder contains real PDPA data** — 900-line seeder hard-gated to `APP_ENV=local` but physically present and could be regenerated | `database/seeders/IRLSeeder.php` | **HIGH** | PDPA, Security | 3-layer enforcement: `app()->environment()` check, `APP_DEBUG=false` check, and documentation. Contains real trainee/centre data for Gombak centre. Cannot execute outside local env per code guard. | Verify env gate is bypass-proof (check `artisan tinker`, queue worker env mismatch). Audit git history for IRLSeeder output. Confirm staging env blocks it per `STAGING_SEED_POLICY.md`. |
| CF-07 | **72 Malaysian IC patterns in git history** — 131 commits scanned, 72 IC-pattern matches on `Fixers` branch | Git history on `Fixers` branch | **HIGH** | PDPA, Security | Audit log (`docs/audit/git_history_audit_2026-05-01.log`) confirms: 72 `######-##-####` patterns across history. 0 `.env` files committed. 0 real AWS keys. 12,168 files under `archive/` ever tracked. | Deferred to post-delivery cleanup per `KNOWN_LIMITATIONS_2026-05-04.md`. Requires `git filter-branch` or BFG Repo-Cleaner. Disruptive — every clone breaks. Must be planned with stakeholder coordination. |
| CF-08 | **Session data logged on every authenticated request** — `MainController.php` logs `session()->all()` including email and IIUM ID at debug level | `app/Http/Controllers/MainController.php` (lines 359, 369, 441, 516, 563, 612, 716, 721, 736, 738), `NotificationController.php` (lines 323, 349), `TraineeHomeController.php` (lines 38, 44, 55, 123) | **HIGH** | Security, PDPA | Flagged as C2 by `DELTA_REEVAL_REPORT_2026-03-22`. `PRE_DEPLOY_SECURITY_CHECKLIST` item 5.3: marked YELLOW. Safe only if production `LOG_LEVEL=warning` or higher. | Verify `.env.production` has `LOG_LEVEL=warning`. If not, implement before any production push. Alternative: replace `Log::debug()` with `Log::info()` and strip PII fields (email, IIUM ID) from log context. |
| CF-09 | **Overly permissive CORS configuration** — `allowed_origins: ['*']`, all methods, all headers | `config/cors.php` | **HIGH** | Security | Applies to `api/*` and `sanctum/csrf-cookie` paths. Current auth is session-based (not API token), limiting practical risk. If API tokens are later added for mobile app, this becomes a serious vulnerability. | Restrict to known production origins before API token-based auth is introduced. Add as a RED item in `PRE_DEPLOY_SECURITY_CHECKLIST.md`. |
| CF-10 | **APP_KEY placeholder in `.env.production`** — contains `base64:GENERATE_NEW_KEY_WITH_php_artisan_key:generate` instead of a real key | `.env.production` (APP_KEY line) | **HIGH** | Deployment, Security | Flagged as RED blocker B1 in `PRE_DEPLOY_SECURITY_CHECKLIST`. Without a real key, sessions, CSRF tokens, and encrypted cookies will not function in production. | Generate real key on production server: `php artisan key:generate`. Store in Lightsail environment or secrets manager. Never commit the real key. |
| CF-11 | **`.env.testing` contains real database password** — `DB_PASSWORD=[REDACTED-CF03]` committed to repository | `.env.testing` | **HIGH** | Credentials, Security | Flagged as H6 by `DELTA_REEVAL_REPORT_2026-03-22`. Was visible in git at time of delta audit. Password appears to be a personal password reused across environments. | Verify if password has been rotated since Mar 2026 audit. Replace with placeholder `DB_PASSWORD=test_password`. Add `.env.testing` to `.gitignore` if not already covered. |
| CF-12 | **Production live UAT shows 2 persistent blockers** — logout doesn't terminate sessions, trainee creation broken. Multiple May 15-18 reports all show FAIL | `docs/audit/live_*_uat_*` (13 files, May 15-18, 2026) | **HIGH** | Deployment, Security | Latest gate smoke (May 17): all 4 roles login, asset detail 200, no 500s. But logout FAIL — sessions persist after logout allowing re-entry without credentials. Trainee creation 500 error. Full browser UAT (May 18): 3,558 controls inventoried. Same 2 blockers. | Fix logout: verify `Session::flush()` + `Session::regenerate()` in logout handler. Fix trainee creation: check foreign key constraints and default values. Re-run full browser UAT after fixes. |
| CF-13 | **6 memory checkpoint files are empty stubs** — the checkpointer fired but no context was saved, creating false continuity during resume | `.memsearch/memory/2026-05-{03,10,14,15,18,19}.md` | **MEDIUM** | Documentation, Technical Debt | 6 of 13 memory files contain only empty template stubs or 3-line entries. Future AI sessions running resume protocol will receive incomplete context from these dates, potentially causing repeated work or missed state. | Review git log for each date to determine if important work occurred. If yes, backfill from git log + `docs/ai-context/`. If no (idle days), mark each with "No activity — idle day" to prevent false gap alerts. |
| CF-14 | **Root-level temp files from route auditing never cleaned up** — temporary JSON files at repository root | `tmp_routes_audit.json`, `tmp_creams_routes.json`, `routes_export.json`, `routes_analysis.json` | **MEDIUM** | Technical Debt, Documentation | `tmp_routes_audit.json` contains valid 629-route inventory. `routes_export.json` is garbage (Laravel error about `--columns` option). `tmp_creams_routes.json` is another export variant. `routes_analysis.json` is a route analysis output. All at root level. | Archive `tmp_routes_audit.json` and `routes_analysis.json` to `docs/audit/`. Delete `routes_export.json` (junk). Clean root directory. |
| CF-15 | **PHPUnit CI uses SQLite :memory: while local uses MySQL** — test environment divergence risks tests that pass locally but fail in CI (or vice versa) | `phpunit-ci.xml` vs `phpunit.xml` | **MEDIUM** | Code Quality, Deployment | CI: `DB_CONNECTION=sqlite`, `DB_DATABASE=:memory:`. Local: `DB_CONNECTION=mysql`, `DB_DATABASE=cream_test`. `DELTA_REEVAL` notes this as acceptable per standard Laravel practice, but edge cases (JSON columns, fulltext indexes, spatial types) may not be caught in CI. | Add optional MySQL service container to CI workflow for parity. At minimum, document known edge case categories and tag relevant tests to run against both databases. |
| CF-16 | **221 N+1 queries on activity schedule page** — 19.5s load time documented in multiple sources | `docs/Validate/PERFORMANCE_BASELINE_METHODOLOGY.md` (line 39), `docs/Validate/MASTER_PROGRESS_LOG.md` | **MEDIUM** | Technical Debt, Performance | Trainee creation: 26s (email notification + N+1 enrollment queries). Schedule page: 19.5s (221 unoptimized queries). Target: <5s and <3s respectively. These are Phase 3 deferred items. | Audit `ActivityController::schedule()` and `TraineeController::store()` for N+1 patterns. Add eager loading (`with()`). Queue email notifications. Use bulk inserts for enrollments. Measure after fix. |
| CF-17 | **GombakDataExtractor.php can regenerate PDPA backup on demand** — the source of `real_data_backup.json` | `database/seeders/GombakDataExtractor.php` | **MEDIUM** | PDPA, Security | Flagged by `DELTA_REEVAL` as H3 (Mar 2026). If executed, regenerates `real_data_backup.json` with CURRENT data from the database — more dangerous than the stale file already on disk. | Delete or add env gate (`if (!app()->environment('local')) { throw... }`) matching IRLSeeder pattern. The backup file already exists; regeneration would increase risk. |
| CF-18 | **Hardcoded IC numbers in TestingGuideDataSeeder** — literal Malaysian IC pattern strings, not Faker-generated | `database/seeders/TestingGuideDataSeeder.php` | **MEDIUM** | PDPA, Security | Flagged as H5 by `DELTA_REEVAL` (Mar 2026). Contains `######-##-####` format as literal strings. Even if intentionally synthetic, the pattern triggers PDPA grep gates and could cause false positives in IC scans. | Replace literal strings with `fake()->numerify('######-##-####')`. Verify pre-commit hook does not generate false positives on Faker-generated IC patterns. |
| CF-19 | **No `.gitignore` coverage for `real_data_backup.json`** — verification needed that file is excluded from version control | Repository root | **MEDIUM** | PDPA | File exists on disk. Presence on disk does not confirm it's tracked. Must verify via git commands. If tracked, it's been committed to history and requires full BFG cleanup. | Run `git ls-files -- database/real_data_backup.json`. If tracked: add to `.gitignore` AND plan BFG cleanup of git history. If untracked: add to `.gitignore` for future safety. |
| CF-20 | **`config/sanctum.php` exists but custom session auth is used** — dormant configuration may mislead developers about the active auth stack | `config/sanctum.php` | **LOW** | Architecture, Technical Debt | Multiple stale docs (old CLAUDE.md) claim auth uses Breeze+Sanctum. The config file's presence reinforces the false impression. No active Sanctum routes exist — auth is handled by `MainController@check` via custom session guard. | Document in architecture inventory that Sanctum config is dormant. Remove the file only after grepping for `Sanctum::` and `Laravel\Sanctum` references across all `app/` and `routes/` code to confirm no dependencies exist. |

---

## Summary Statistics

| Risk Level | Count | Primary Categories |
|-----------|-------|-------------------|
| **CRITICAL** | 4 | PDPA (3), Credentials (2), Security (2), Infrastructure (1) |
| **HIGH** | 8 | PDPA (3), Security (5), Credentials (3), Deployment (3) |
| **MEDIUM** | 7 | PDPA (2), Security (1), Technical Debt (3), Documentation (1), Performance (1) |
| **LOW** | 1 | Architecture, Technical Debt |
| **TOTAL** | **20** | |

---

## Remediation Priority (for future phases)

| Priority | Findings | Justification |
|----------|----------|---------------|
| **IMMEDIATE** | CF-01, CF-02, CF-03, CF-04 | PDPA compliance — real data exposure on disk and in screenshots |
| **BEFORE DEPLOY** | CF-08, CF-10, CF-11, CF-12 | Blockers for production push |
| **BEFORE API/MOBILE** | CF-09 | CORS restriction needed before API tokens |
| **POST-DELIVERY** | CF-05, CF-06, CF-07, CF-17, CF-18, CF-19 | Historical cleanup, git history rewrite |
| **NICE-TO-HAVE** | CF-13, CF-14, CF-15, CF-16, CF-20 | Operational hygiene, documentation completeness |

---

*Generated by automated repository exploration. Document only — no fixes applied. No files modified. No files deleted. Use during later security and remediation phases.*
